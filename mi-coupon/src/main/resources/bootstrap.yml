spring:
  application:
    name: mi-coupon
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: gitdlq.top:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        namespace: a093a601-dfe5-4bec-bff1-8bae00d7761b

